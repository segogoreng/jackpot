export class GameConstants {
    static readonly GAME_WIDTH = 1200;
    static readonly GAME_HEIGHT = 800;
    static readonly GAME_CENTER_X = GameConstants.GAME_WIDTH / 2;
    static readonly GAME_CENTER_Y = GameConstants.GAME_HEIGHT / 2;
}
