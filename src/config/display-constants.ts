export class DisplayConstants {
    static readonly GAME_WIDTH = 1200;

    static readonly GAME_HEIGHT = 800;

    static readonly GAME_CENTER_X = DisplayConstants.GAME_WIDTH / 2;

    static readonly GAME_CENTER_Y = DisplayConstants.GAME_HEIGHT / 2;
}
